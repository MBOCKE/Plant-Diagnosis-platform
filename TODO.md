# TODO

- [ ] Replace the leaf icon in the login header of the app with the user-provided logo (PlantDiagnosisApp54).
- [ ] Ensure the logo asset is added to the correct frontend assets folder.
- [ ] Update `5_frontend/PlantDiagnosisApp54/app/(auth)/login.tsx` to render the logo image instead of the Ionicons leaf.
- [ ] Build / run sanity checks for the frontend (TypeScript compile).

