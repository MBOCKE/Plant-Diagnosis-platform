#!/bin/bash
set -e

echo "Stopping all backend services..."
# Stop each service or use a process manager
# Example:
# pkill -f "node .*auth-service"
# pkill -f "node .*treatment-service"
# pkill -f "node .*case-service"
# pkill -f "node .*inference-service"
# pkill -f "node .*api-gateway"
